/**
 * Apenas um ícone de loading, que pode ser usado em botões ou em qualquer outro lugar onde seja necessário indicar que algo está carregando.
 */
export const SpinIcon = () => {
	return (
		<div className="w-4 h-4 border-2 border-white/30 border-t-white rounded-full animate-spin" />
	);
};
